# Port for the local backend server.
PORT=8787

# OpenAI API key used only by the backend server.
# Keep this private and never commit real keys.
OPENAI_API_KEY=your_openai_api_key_here

# Model used by backend AI calls.
OPENAI_MODEL=gpt-4o-mini
